/// AoC Test Utilities
/// Provides reusable infrastructure for Advent of Code solutions
///
/// This module defines the protocol prototype that all daily solutions
/// should implement for consistent testing.

/// Protocol prototype for AoC solution modules
/// All daily solutions should implement this interface for consistent testing
///
/// Interface:
///   clk      - Clock input
///   rst      - Reset input (active low)
///   i_data   - 8-bit input data stream (one byte at a time)
///   i_valid  - Input valid signal
///   o_answer - 64-bit output answer
///   o_busy   - Output busy signal (backpressure)
///   o_valid  - Output valid signal (result ready)
pub proto module ProtoAocSolution (
    clk: input clock,
    rst: input reset,

    i_data : input logic<8>,
    i_valid: input logic   ,

    o_answer: output logic<64>,
    o_busy  : output logic    ,
    o_valid : output logic    ,
);
